# Web App

React, TypeScript, Vite, Ant Design, and Recharts.

This app lives at `apps/web` inside the monorepo. During development, Vite proxies `/api` to the Go API service on `localhost:8080`.

Run:

```bash
npm install
npm run dev
```

Build:

```bash
npm run build
```
